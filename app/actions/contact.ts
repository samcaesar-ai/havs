"use server";

// Segment 8 will implement: Sheets append + Resend emails
export async function submitContact(_prev: unknown, _formData: FormData) {
  return { success: false, error: "Not yet implemented" };
}
